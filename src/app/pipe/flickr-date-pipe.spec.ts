import { FlickrDatePipe } from './flickr-date-pipe';

describe('FlickrDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FlickrDatePipe();
    expect(pipe).toBeTruthy();
  });
});
